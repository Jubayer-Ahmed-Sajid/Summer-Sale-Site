function getCardPrice(textId) {
    const cardPrice = document.getElementById(textId);
    const cardPriceString = cardPrice.innerText;
    const price = parseFloat(cardPriceString);
    return price;
}

function couponCode(){
 
}
